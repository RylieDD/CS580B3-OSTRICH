sampling.ipynb does the sampling requiring `diff_sorted_msr_data_no_overflow_PRs(in).csv` in the same directory. It requires no dependencies beyond python.

The pull request numbers representing the quarters are in first.txt, second.txt, third.txt, fourth.txt.

The train, validation, and test set pull request numbers are in: train.txt, validate.txt, test.txt. 